version: 1
applications:
  - appRoot: web/fleet-dispatch-web
    frontend:
      phases:
        preBuild:
          commands:
            - npx prisma generate
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
